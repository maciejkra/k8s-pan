# Zadanie

1. Zaaplikuj `google-service.yaml` (typu `ExternalName`) i z Pod-a wykonaj `curl google-service`. Co dokładnie zwraca CoreDNS?
2. Wypisz domyślne zmienne środowiskowe Service w Pod-zie (`env | grep _SERVICE_`).
3. Odpowiedz: co się stanie ze zmiennymi env, jeśli Service powstanie *po* uruchomieniu Pod-a?
